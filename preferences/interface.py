# real impl lives in preferences/impl.py; we use the stub until then


def get_enabled_channels(user_id: str) -> list[str]:
    raise NotImplementedError(
        "Use preferences_stub.get_enabled_channels or preferences.impl.get_enabled_channels"
    )
